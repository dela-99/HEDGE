{
  "name": "hedge",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "build": "npm run build --workspace apps/auth-service",
    "start:dev": "npm run start:dev --workspace apps/auth-service",
    "test": "npm run test --workspace apps/auth-service --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
